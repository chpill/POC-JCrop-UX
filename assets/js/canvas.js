// Generated by CoffeeScript 1.6.2
(function() {
  var Canvas,
    __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

  Canvas = (function() {
    function Canvas(domElement) {
      this.draw = __bind(this.draw, this);      this.context = domElement.getContext("2d");
      this.el = domElement;
    }

    Canvas.prototype.draw = function() {
      this.context.fillStyle = 'rgba(150, 130, 150, 1)';
      this.context.fillRect(0, 0, this.el.width, this.el.height);
      this.context.fillStyle = 'rgba(0, 0, 0, 1)';
      this.context.fillRect(200, 200, 100, 150);
      this.context.fillStyle = 'rgba(255, 255, 255, 1)';
      return this.context.fillRect(50, 100, 300, 100);
    };

    return Canvas;

  })();

  this.Canvas = Canvas;

}).call(this);
